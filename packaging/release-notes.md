丸丸小帮手 v0.3.2 Windows 自用版。EXE 未做 Authenticode 签名，请从本仓库 Release 下载并核对 SHA-256 清单。

本次修复自动更新后的重启：新版单文件 EXE 会重新解包依赖，避免沿用旧版已删除的临时目录而出现 `python314.dll` 加载失败。通信簿、照片批量调整、Word 多图排版及文档激进瘦身继续提供。

**从旧版升级的过渡说明：**旧版更新器自身尚未包含此修复，替换为 v0.3.2 后第一次自动重启仍可能出现上述错误。关闭错误框，再从原位置手动打开程序即可；也可以直接下载本版 EXE 使用。打开后可在窗口标题确认版本为 v0.3.2。此后的自动更新将使用修复后的重启流程。

下载 `WanwanTeacherHelper-*-windows-x64.exe` 即可运行，无需安装 Python、Office 或 FFmpeg。原件只读，先预览再处理，结果另存；同名输出自动编号。

Windows CI 执行功能与界面测试，并对目录版、单 EXE、普通账户离线环境进行验收。验证结果、界面截图、SBOM、第三方许可、未签名状态及 SHA-256 清单随发行提供。

真实 Office/WPS 画面、字体、动画、播放、打印及园所现场仍需在对应环境验收。PDF 不保留书签，表单、签名、注释和链接不处理；视频使用 MPEG-4/AAC。请先用材料副本试用并复核输出。
